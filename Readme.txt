How to start this application. 

1. On your terminal, enter the directory that has this code. 
2. Inside the directory from the terminal, use command 'node userManager.js' or 'nodemon userManager.js'. 
3. Once it logs 'Listening on port 4000 Connected, database is up', go to your browser and go to http://localhost:4000. 

